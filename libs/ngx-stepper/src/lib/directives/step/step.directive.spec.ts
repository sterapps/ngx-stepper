import { NgxStep } from './step.directive';

describe('StepDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxStep();
    expect(directive).toBeTruthy();
  });
});
